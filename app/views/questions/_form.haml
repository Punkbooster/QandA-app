= simple_form_for [@event, @event.questions.build], remote: true do |f|
	= f.input :content, label: 'Your question:', id: 'question_content'
	= f.submit class: 'submit_btn'

-# = form_for([@event, @event.questions.build], remote: true) do |f|
-# 	.field
-# 		= f.label :question
-# 		%br
-# 			= f.text_field :content
-# 	.actions
-# 		= f.submit
