#question_container
	%p
		= question.content

		= link_to "", like_admin_event_question_path(@event, question), method: :put,class:'glyphicon glyphicon-thumbs-up'
		= question.score
		= 'Vote'.pluralize(question.score)

	= link_to "Edit", edit_admin_event_question_path(question.event, question), class: "buttons"
	= link_to "Delete", [question.event, question], method: :delete, data: {confirm: "Are you sure?"}, remote: true, class: 'delete-question buttons'
